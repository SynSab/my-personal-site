const CONFIG = {


    // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    // HOMEPAGE
    // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    homepage: {

        intro: {
            // Controls the settling animation at the end of the wheel spin-down
            // 'bounce'    — word bounces up slightly off the landing platform
            // 'overshoot' — word drifts just past the platform, reverses back
            settleMode: 'bounce',
        },

    },


    // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    // TIES 2  (March 28)
    // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    ties2: {

        design: {
            // Controls which Ties 2 page design is linked from the homepage
            // 'minimal' — matches the main site aesthetic (warm off-white, Cormorant)
            // 'green'   — uses the original Ties green aesthetic
            version: 'minimal',
        },

    },


};
